/*CH-230-A
A1p2.c
Kamila Ziza
kziza@constructor.university
*/

#include <stdio.h>
int main() {
int result; /* The result of our calculation */
result = (2 + 7) * 9 / 3;
printf("The result is %d\n", result);
return 0;
}
// %d is a format specifier, so it shows a intiger but if we do not put the variable name the correct result will not be printed, instead a random selection of numbers will appear, and we sholud be careful with the kind of asterix(*) we use for comments and what we use for multiplications.